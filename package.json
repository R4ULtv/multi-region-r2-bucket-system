{
  "name": "multi-region-bucket-cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.5",
    "geolib": "^3.3.4",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
